node_modules/
.nuxt/
.output/
dist/
.env
.env.local
*.log
.DS_Store
Thumbs.db

# Playwright MCP scratch (screenshots/snapshots from local rendering)
.playwright-mcp/

# Claude Code — version the shared agent definitions, ignore machine-local state
.claude/*
!.claude/agents/

# Raw BGG data dump — only the derived server/data/board-games.json is committed
/data/
