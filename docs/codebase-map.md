# Codebase Map

> Last updated: 2026-06-04 · commit `183856e`
> Generated by doc-worker. Update this file when files are added, removed, or
> their purpose changes. Do not add entries for node_modules or generated files.

## Config & entry points

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `nuxt.config.ts` | Nuxt 3 config: Tailwind v4 Vite plugin, public `buildNumber` (git-timestamp scheme), PWA head, Nitro externals | All |
| `drizzle.config.ts` | Drizzle Kit config pointing at the migrations folder and Postgres | All |
| `vitest.config.ts` | Vitest config for unit and co-located service tests | All |
| `playwright.config.ts` | Playwright config for e2e smoke tests (base URL, reporter) | All |
| `docker-compose.yml` | Two-service compose: `app` (Nuxt/Nitro) + `db` (Postgres 16 alpine), named `photos` and `pgdata` volumes | Deployment |
| `Dockerfile` | Multi-stage build: Node 22 builder (with git for version stamp) + slim runtime image | Deployment |

## App — Layouts

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/layouts/default.vue` | Public layout: sticky glass header (wordmark + build version), mesh background, availability-note footer | M1 / M5 polish |
| `app/layouts/detail.vue` | Full-screen game detail layout: sticky header with a "← Catalogue" back link instead of the wordmark | M5 — detail page redesign |
| `app/layouts/staff.vue` | Staff layout: flat white nav bar (brand underline active link, hamburger on mobile), sign-out link | M2 / M5 polish |

## App — Pages (public)

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/pages/index.vue` | Public catalogue root: thin wrapper that passes fetched games/tags to `CatalogueBrowser` | M1 |
| `app/pages/games/[id].vue` | Game detail page: glass hero (full-bleed photo + scrim + title overlay), photo lightbox, sticky two-column on desktop | M1 / M5 polish |

## App — Pages (staff)

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/pages/staff/login.vue` | Staff login page: 360px card, scoped CSS layout, username/password form | M2 / M5 polish |
| `app/pages/staff/index.vue` | Staff games dashboard: responsive table (desktop) + card list (mobile), featured toggle, segmented filter, quick search | M2 / M4 / M5 polish |
| `app/pages/staff/catalogue.vue` | Auth-gated staff preview of the public catalogue (same `CatalogueBrowser`, distinct `useAsyncData` keys) | M5 |
| `app/pages/staff/games/new.vue` | Add-game page: wraps `GameForm` | M2 |
| `app/pages/staff/games/[id]/edit.vue` | Edit-game page: fetches game by id, wraps `GameForm` | M2 |
| `app/pages/staff/tags/index.vue` | Tag management: list all tags, inline rename (pencil icon), merge, archive/restore | M4 / M5 polish |
| `app/pages/staff/tags/[id].vue` | Tag game-management page: lists games carrying the tag, add/remove game membership | M5 |
| `app/pages/staff/users/index.vue` | Admin user management: create accounts, reset passwords, delete (admin only) | M4 |

## App — Components — Catalogue

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/components/catalogue/CatalogueBrowser.vue` | Shared catalogue body (props-driven): featured strip, filter sidebar, search, card grid / list view toggle | M5 — extracted from index.vue |
| `app/components/catalogue/CatalogueFilters.vue` | Glass filter chips for player count, play time, and tags | M1 / M5 polish |
| `app/components/catalogue/FeaturedStrip.vue` | Mobile horizontal scroll-snap carousel of featured games (auto-advance off, drag-scroll on desktop) | M5 |
| `app/components/catalogue/GameCard.vue` | Editorial card: photo-as-background, scrim, frosted info panel, brand-gradient no-photo fallback | M1 / M5 polish |
| `app/components/catalogue/GameListItem.vue` | Dense list row: thumbnail, name, meta, tags | M1 / M5 polish |
| `app/components/catalogue/PhotoLightbox.vue` | Full-screen photo viewer with swipe/zoom/keyboard navigation; staff `editable` mode adds rotate/delete toolbar | M5 |

## App — Components — Staff

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/components/staff/GameSearch.vue` | Debounced type-ahead search against the local `board-games.json` name list; emits `GameSearchResult` on selection | M3 / M5 polish |
| `app/components/staff/FeaturedReplaceModal.vue` | Modal for the "at-cap replace" flow: radio list of current 3 featured games with thumb/relative-time chip | M5 — featured toggle |
| `app/components/staff/FeaturedToggle.vue` | Inline star button to feature/unfeature a game from the staff list (shared by table and mobile cards) | M5 — featured toggle |
| `app/components/staff/GameForm.vue` | Add/edit game form: name, description, RangeSlider players/time, TagTypeahead, PhotoUpload, Wikipedia fetch | M2 / M3 / M5 polish |
| `app/components/staff/PhotoUpload.vue` | Photo management UI: upload dropzone (file + mobile camera capture), drag-to-reorder, tap-to-view lightbox | M2 / M5 |
| `app/components/staff/TagTypeahead.vue` | Tag picker with inline new-tag creation used in `GameForm` | M2 |

## App — Components — Shared

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/components/shared/Button.vue` | Primary and danger button variants; optional `to` prop renders as a `NuxtLink` | M2.5 |
| `app/components/shared/FeaturedBadge.vue` | Presentational glass-pill badge with star icon; placement is the parent's responsibility | M2.5 |
| `app/components/shared/Input.vue` | Text input with v-model, `invalid` prop for error state, full attr inheritance | M2.5 |
| `app/components/shared/RangeSlider.vue` | Dual-thumb range slider (pointer + keyboard): `v-model:low` / `v-model:high` over `[min, max]` | M5 — game form |
| `app/components/shared/SearchInput.vue` | Search box with magnifier icon and clear button; used in staff games/tags/users lists | M5 |

## App — Composables

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/composables/useFilters.ts` | Client-side filter state for the catalogue (search, player count, play time, tags, featured) | M1 |
| `app/composables/useCatalogueView.ts` | Persists the card-grid vs. list-view toggle to `localStorage` | M1 |

## App — Middleware & utils

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/middleware/auth.ts` | Client route guard: redirects unauthenticated users to `/staff/login` using `useRequestFetch()` | M2 |
| `app/middleware/admin.ts` | Client route guard: restricts admin-only pages to the `admin` role | M4 |
| `app/utils/relativeTime.ts` | Formats a Date/ISO string as a compact relative string ("5m ago", "3 days ago") for staff UI | M5 — featured toggle |

## Server — API (public)

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `server/api/health.get.ts` | Health-check endpoint: returns `{ ok: true, db: 'connected' }` after pinging Postgres | M0 |
| `server/api/games/index.get.ts` | Lists all visible (non-deleted) games with tags and first photo hash | M1 |
| `server/api/games/[id].get.ts` | Returns a single game by id with full photo list | M1 |
| `server/api/tags/index.get.ts` | Lists all active (non-archived) tags | M1 |
| `server/api/photos/[hash]/[file].get.ts` | Serves processed photo files from the photos volume | M1 |
| `server/api/game-names/search.get.ts` | In-memory substring search over `board-games.json`; returns up to 20 results ranked by popularity | M3 (impl. replaced M3) |
| `server/api/wikipedia/info.get.ts` | Fetches game description + players + play time from Wikipedia's open API | M5 — Wikipedia enrichment |

## Server — API (staff / auth)

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `server/api/auth/login.post.ts` | Validates credentials (Argon2id), creates session cookie | M2 |
| `server/api/auth/logout.post.ts` | Invalidates the current session | M2 |
| `server/api/auth/me.get.ts` | Returns the current session user (used by client auth middleware) | M2 |
| `server/api/staff/games/index.get.ts` | Lists all games (including soft-deleted) for the staff dashboard | M2 |
| `server/api/staff/games/index.post.ts` | Creates a new game with audit fields | M2 |
| `server/api/staff/games/[id]/index.patch.ts` | Updates an existing game | M2 |
| `server/api/staff/games/[id]/index.delete.ts` | Soft-deletes a game | M2 |
| `server/api/staff/games/[id]/restore.post.ts` | Restores a soft-deleted game | M4 |
| `server/api/staff/games/[id]/photos.post.ts` | Uploads and processes a photo through the sharp pipeline | M2 |
| `server/api/staff/games/[id]/photos/[hash].delete.ts` | Deletes a photo file and its DB row (only if no other game references the hash) | M5 — photo management |
| `server/api/staff/games/[id]/photos/[hash]/rotate.post.ts` | Rotates a photo 90° CW or CCW via sharp, updates the content hash | M5 — photo management |
| `server/api/staff/games/[id]/photos/reorder.post.ts` | Persists a new photo display order (position column) | M5 — photo management |
| `server/api/staff/games/[id]/feature.post.ts` | Marks a game as featured; optional `{replace}` body for atomic 3-slot swap | M5 — featured toggle |
| `server/api/staff/games/[id]/unfeature.post.ts` | Removes a game's featured status | M5 — featured toggle |
| `server/api/staff/games/featured-count.get.ts` | Returns the current count of featured games (used to enforce the max-3 cap) | M4 |
| `server/api/staff/stats.get.ts` | Returns total counts of games, tags, and users for the staff dashboard | M5 |
| `server/api/staff/tags/index.get.ts` | Lists all tags (including archived) for staff management | M4 |
| `server/api/staff/tags/[id]/index.get.ts` | Returns a single tag with its associated games | M5 |
| `server/api/staff/tags/[id]/index.patch.ts` | Renames a tag | M4 |
| `server/api/staff/tags/[id]/index.delete.ts` | Archives a tag (sets `archived_at`; does not remove from existing game_tags) | M4 |
| `server/api/staff/tags/[id]/merge.post.ts` | Merges all games from the source tag onto a target tag, then deletes the source | M4 |
| `server/api/staff/tags/[id]/restore.post.ts` | Unarchives a tag | M4 |
| `server/api/staff/tags/[id]/games.post.ts` | Adds a game to a tag | M5 — tag game manager |
| `server/api/staff/tags/[id]/games.delete.ts` | Removes a game from a tag | M5 — tag game manager |
| `server/api/staff/users/index.get.ts` | Lists all staff accounts (admin only) | M4 |
| `server/api/staff/users/index.post.ts` | Creates a new staff account (admin only) | M4 |
| `server/api/staff/users/[id]/index.delete.ts` | Deletes a staff account (admin only, self-delete guarded) | M4 |
| `server/api/staff/users/[id]/reset-password.post.ts` | Resets a staff account password (admin only) | M4 |

## Server — DB schema

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `server/db/schema/index.ts` | Re-exports all schema tables | All |
| `server/db/schema/health-check.ts` | `health_check` table used by the M0 migration to verify the pipeline | M0 |
| `server/db/schema/games.ts` | `games` table: name, description, players, time, bggId, featured/featuredAt/featuredBy, audit columns, soft-delete | M1 / M4 / M5 |
| `server/db/schema/photos.ts` | `photos` table: gameId FK, contentHash, position (for ordering) | M1 / M5 |
| `server/db/schema/tags.ts` | `tags` and `game_tags` tables; `archived_at` for soft-delete | M1 / M4 |
| `server/db/schema/auth.ts` | `users` and `sessions` tables (Lucia auth) | M2 |

## Server — DB migrations

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `server/db/migrations/0000_initial_health_check.sql` | Creates the `health_check` table | M0 |
| `server/db/migrations/0001_tough_pride.sql` | Creates `games`, `tags`, `game_tags`, `photos` tables | M1 |
| `server/db/migrations/0002_stiff_stick.sql` | Creates `users`, `sessions` tables | M2 |
| `server/db/migrations/0003_bgg_cache.sql` | Creates `bgg_games_cache` table (now unused) | M3 (retired) |
| `server/db/migrations/0004_true_absorbing_man.sql` | Adds nullable `featured_at`, `featured_by_id` to `games` | M5 — featured toggle |
| `server/db/migrations/0005_breezy_blue_blade.sql` | Drops the unused `bgg_games_cache` table | M3 (cleanup) |

## Server — DB queries

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `server/db/queries/games.ts` | All game query functions: list/get for public and staff, create, update, soft-delete, restore, featured management, photo queries | M1–M5 |
| `server/db/queries/tags.ts` | Tag query functions: list active/all, create, rename, archive, merge, get by id, game membership | M1–M5 |
| `server/db/queries/users.ts` | User query functions: get by username/id, create, update password, delete | M2 / M4 |

## Server — Services

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `server/services/auth.ts` | Lucia wrapper: password hashing/verification (Argon2id), session create/delete, `requireAuth`/`requireAdmin` guards | M2 |
| `server/services/photos.ts` | Sharp pipeline: process uploaded/fetched images into thumb/card/detail sizes (WebP + JPEG), EXIF auto-orient, rotate, delete | M1 / M5 |
| `server/services/photos.test.ts` | Unit tests for the photo service | M1 |
| `server/services/wikipedia.ts` | Wikipedia API client: searches for a game article, confirms `{{Infobox game}}`, returns description + player/time ranges | M5 — Wikipedia enrichment |
| `server/services/wikipedia.test.ts` | Unit tests for the Wikipedia service (match-guard tests) | M5 |

## Server — Utils & plugins

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `server/utils/gameNames.ts` | Loads `board-games.json` in-memory, exposes `searchGameNames` ranked by popularity | M3 (impl. replaced M3) |
| `server/plugins/migrations.ts` | Nitro plugin: runs `drizzle-kit migrate` on server start via an isolated Postgres client | M0 |

## Server — Data

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `server/data/board-games.json` | Static committed list of ~42k board games (name, bggId, yearPublished, usersRated) from BGG ranks export | M3 (impl. replaced M3) |

## Scripts

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `scripts/seed.ts` | Seeds ~20 test games with placeholder photos; idempotent; runs on dev container start when `AUTO_SEED=true` | M1 |
| `scripts/seed-admin.ts` | Creates or syncs the admin account from `ADMIN_USERNAME`/`ADMIN_PASSWORD` env vars; runs on container start when vars are set | M2 |
| `scripts/convert-bgg-ranks.ts` | One-time script: converts BGG's `boardgames_ranks.csv` dump into `server/data/board-games.json` | M3 (impl. replaced M3) |
| `scripts/generate-icons.ts` | One-time script: generates PWA icon PNGs at 16/32/180/192/512px into `public/` | M1 |

## Tests

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `tests/unit/health.test.ts` | Vitest unit test for the health-check route | M0 |
| `tests/e2e/catalogue.spec.ts` | Playwright smoke: customer loads catalogue, filters, opens detail page | M1 |
| `tests/e2e/staff.spec.ts` | Playwright smoke: staff login → add game → verify on catalogue | M2 |
| `server/services/photos.test.ts` | Co-located unit tests for the photos service | M1 |
| `server/services/wikipedia.test.ts` | Co-located unit tests for the Wikipedia service | M5 |

## Design & docs

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `requirements.md` | Functional and non-functional requirements; users, out-of-scope items | All |
| `ARCHITECTURE.md` | System shape, technology choices, data model, deployment, architectural non-goals | All |
| `CONVENTIONS.md` | Directory layout, TypeScript rules, DB access patterns, component rules, naming, testing | All |
| `README.md` | Project overview and documentation index | All |
| `CLAUDE.md` | Agent session startup instructions (pull, read docs in order) | All |
| `docs/roadmap.md` | Phased delivery milestones M0–M5 with scope and done-when criteria | All |
| `docs/progress.md` | Granular session log: what's done, what's next, deployment infra details | All |
| `docs/design-brief-context.md` | Pre-M5 token snapshot — **stale**; superseded by `tokens.css` and `design/HANDOFF.md` | M5 (historical) |
| `docs/codebase-map.md` | This file — one-row-per-file codebase inventory maintained by doc-worker | All |
| `docs/doc-worker-state.md` | Doc-worker session state: onboarding status, queue, deferred updates, opened issues | All |
| `design/DIRECTIONS.md` | Three M5 visual direction candidates (A Café Warm / B Felt & Slate / C Game Night); B was chosen | M5 |
| `design/HANDOFF.md` | M5 design implementation spec: two-layer model (tokens + treatments), Glass Slate public + flat admin | M5 |
| `design/concepts/README.md` | Index of design-worker concept files | M5 |
| `design/concepts/mobile-featured-presentation.md` | Mobile featured section concept — Concept A (carousel) implemented as `FeaturedStrip.vue` | M5 (implemented) |
| `design/concepts/staff-featured-toggle.md` | Staff featured toggle + replace-modal concept — implemented as `FeaturedToggle.vue` + `FeaturedReplaceModal.vue` | M5 (implemented) |

## App — Assets

| File | Purpose | Milestone / feature |
|------|---------|---------------------|
| `app/assets/css/tokens.css` | Design tokens: Felt & Slate palette (incl. `--color-focus-ring`/`--color-error-ring` for input/button focus rings), glass tokens, spacing, typography (Inter self-hosted), layout chrome heights, motion, z-index layers, custom `@utility` for `z-header`/`z-toolbar` | M2.5 / M5 |
